# Public — safe to expose to the browser (RLS gates all writes)
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=

# Server-only — never expose to the browser bundle, used only inside
# src/pages/api/admin/* for privileged Storage operations
SUPABASE_SERVICE_ROLE_KEY=
